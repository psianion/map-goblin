import type { Point } from '@/types/geometry';

/**
 * Snap to existing wall endpoints when within threshold.
 * Stub — full implementation in tools-dev track.
 */
export function wallEndpointSnap(point: Point): Point {
  return point;
}
