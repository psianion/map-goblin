// D8 — importing this registers the DM's Prep sidebar and the player's Journal sidebar.
// GameTable learns nothing else (table-shell-redesign: the old rail popover, TriggerPanel.tsx,
// is retired in favor of the sidebar seam, `session/sidebars.ts`).
import './PrepSidebar';
import './JournalSidebar';
