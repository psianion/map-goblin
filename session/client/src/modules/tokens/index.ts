// D8 — the module's single entry point: importing it registers both panels, and the
// shared one mounts the Pixi overlay. GameTable imports this and learns nothing else.
import './TokenPanel';
import './TokenLibraryPanel';
